<link rel="stylesheet" href="{{ asset('css/navbar.css') }}"/>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01"
            aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
        <a class="navbar-brand text-primary" href="{{ path("index") }}">iBook</a>
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item">
                <a class="nav-link" href="{{ path("index") }}">{{ "default.nav.categorie" | trans }}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path("contact") }}">{{ "default.nav.contact" | trans }}</a>
            </li>
        </ul>

        {{ render(controller('App\\Controller\\ShoppingCartController::shoppingWidgetAction')) }}

        <form class="form-inline pl-3" action="{{ path('search') }}" method="post">
            <div class="input-group">
                <input required name="keyword" class="sm-input form-control" type="search" placeholder="Python" aria-label="Search">
                <div class="input-group-append">
                    <button class="btn btn-outline-light" type="submit"><i
                                class="material-icons align-middle">search</i></button>
                </div>
            </div>
        </form>

        <div style="color: white;" class="pr-4 my-lg-0 dropdown pl-3">
            <a role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" id="languageChoice">
                <img src="{{ asset("images/lang-change.svg") }}" />
                <img src="{{ asset('images/down-arrow.svg') }}">
                <div class="dropdown-menu" aria-labelledby="languageChoice">
                    <a class="dropdown-item" href="{{ path("index", {'_locale': 'fr'}) }}">Français</a>
                    <a class="dropdown-item" href="{{ path("index", {'_locale': 'en'}) }}">English</a>
                </div>
            </a>
        </div>
    </div>
</nav>